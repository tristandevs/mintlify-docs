Sign up for an account on [ShuttleAI](https://shuttleai.app) and obtain a FREE API key from your dashboard!

All API requests must include your API key in the Authorization HTTP header as shown below:

> Keep your API key confidential! Do not disclose it to anyone!

<CodeGroup>

```javascript HTTP ShuttleAI API Auth
Authorization: Bearer YOUR_SHUTTLEAI_API_KEY
```

```python Lib ShuttleAI API Auth
from shuttleai import *

"""Synchronous"""
shuttle = ShuttleClient("YOUR_SHUTTLEAI_API_KEY")

# OR

"""Asynchronous"""
async with ShuttleAsyncClient("YOUR_SHUTTLEAI_API_KEY") as shuttle:
```

</CodeGroup>

<Tip>Remember! We strongly advise using the Asynchronous client over the Synchronous client whenever possible.</Tip>
